{
  "name": "@aethelred/shiora-contracts",
  "version": "0.1.0",
  "private": true,
  "description": "Shiora on-chain health-data protocol layer (isolated Hardhat project).",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "coverage": "hardhat coverage"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-chai-matchers": "^2.1.2",
    "@nomicfoundation/hardhat-ethers": "^3.1.3",
    "@nomicfoundation/hardhat-network-helpers": "^1.1.2",
    "chai": "^4.5.0",
    "ethers": "^6.17.0",
    "hardhat": "^2.29.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "5.0.2"
  }
}
